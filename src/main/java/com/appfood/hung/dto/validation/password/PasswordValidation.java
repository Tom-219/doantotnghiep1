/*package com.appfood.hung.dto.validation.password;

import com.appfood.hung.dto.AccountDto;

import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;

public class PasswordValidation implements ConstraintValidator<PasswordAnotation, AccountDto> {
    @Override
    public boolean isValid(AccountDto value, ConstraintValidatorContext context) {
        return value.getPassword().equals(value.getRePassword());
    }
}*/
